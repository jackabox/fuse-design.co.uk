{% set heading = heading ?? '' %}
{% set class = class ?? '' %}
{% set tag = tag ?? 'h1' %}

<{{ tag }} class="{{ twMerge('text-4xl leading-normal font-normal', class) }}">
    {{ heading|raw }}
</{{ tag }}>
