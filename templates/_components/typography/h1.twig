{% set heading = heading ?? '' %}
{% set class = class ?? '' %}
{% set tag = tag ?? 'h1' %}

<{{ tag }} class="{{ twMerge('text-4xl leading-13 font-normal', class) }}">
    {{ heading|raw }}
</{{ tag }}>
