{% set heading = heading ?? '' %}
{% set class = class ?? '' %}
{% set tag = tag ?? 'h1' %}

<{{ tag }} class="{{ twMerge('text-3xl leading-15 font-normal', class) }}">
    {{ heading }}
</{{ tag }}>
