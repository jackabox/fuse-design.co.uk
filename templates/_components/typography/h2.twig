{% set heading = heading ?? '' %}
{% set class = class ?? '' %}
{% set tag = tag ?? 'h2' %}

<{{ tag }} class="{{ twMerge('text-2xl leading-tight font-normal', class) }}">
    {{ heading|raw }}
</{{ tag }}>
